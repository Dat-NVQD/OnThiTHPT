#include<iostream>
#include<fstream>

using namespace std;
#define maxn 10e9;
#define maxm 5*10e5;

void Vxe()
{
    double V,sum[n];
    for(int i(si);i<fi;i++)
    {
        if(ri==0)
        {

        }
    }
}

int main()
{
    freopen("daily.inp","r",stdin);
    freopen("daily.out","w",stdout);

    long long n,m;
    cin >> n >> m;
    double daily[n];for(int i(0);i<n;i++){cin >> daily[i];};
    int si,fi,ci,ri;
    for(int i(0);i<m;i++)
    {
        do
        {
           cin >> si >> fi >> ci >> ri;
        } while(1<=si && si < fi && fi < n);//1 ≤s_i<f_i≤N
        Vxe();
        
    }
    

    return 0;
}