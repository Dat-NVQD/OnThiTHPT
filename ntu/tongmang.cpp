#include<bits/stdc++.h>

using namespace std;

int tong(int n)
{
    int sum = 0, a;
    for(int i(0);i<n;i++)
    {
        cin >> a;
        sum = sum+a;
    }
    return sum;
}

int main()
{
    int n, cin >> n;
    cout << tong;
    return 0;
}